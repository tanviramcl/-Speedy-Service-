<!-- User Account: style can be found in dropdown.less -->
<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    @*<img src="dist/img/user2-160x160.jpg" class="user-image" alt="User Image" />*@
    <span class="hidden-xs" id="accountfullName"></span>
</a>
<ul class="dropdown-menu">
    <!-- User image -->
    @*<li class="user-header">
        <img src="dist/img/user2-160x160.jpg" class="img-circle" alt="User Image" />
        <p>
            Alexander Pierce - Web Developer
            <small>Member since Nov. 2012</small>
        </p>
    </li>
    <!-- Menu Body -->
    <li class="user-body">
        <div class="col-xs-4 text-center">
            <a href="#">Followers</a>
        </div>
        <div class="col-xs-4 text-center">
            <a href="#">Sales</a>
        </div>
        <div class="col-xs-4 text-center">
            <a href="#">Friends</a>
        </div>
    </li>*@
    <!-- Menu Footer-->
    <li>
       
            <a id="accountLink" href="#" class="btn btn-default btn-flat">Edit Account</a>
       </li>
    <li>
        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
        {
            @Html.AntiForgeryToken()

        }
        <a class="btn btn-default btn-flat" href="javascript:document.getElementById('logoutForm').submit()">Log off</a>
    </li>
        
</ul>
<script type="text/javascript">
        $(document).ready(function () {
            $.ajax({
                url: '../Account/GetUser',
                type: 'POST',
                dataType: 'json',
                contentType: 'application/json',
                cache: false,
                data: {},
                success: function (res) {
                    var acName = "Hi "+res.FirstName + " " + res.LastName;
                    if (res.RoleID == 3) {
                        var acLink = '../Requestor/index';
                    } else {
                        var acLink = '../Account/AccountUpdate';
                    }

                    $("#accountfullName").text(acName);
                    $("#accountLink").attr('href', acLink);
                }
            });
        });
</script>